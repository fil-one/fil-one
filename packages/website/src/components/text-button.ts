/**
 * A secondary path stated as plain text ("Not your account? Sign out"). Text
 * buttons carry no chrome, so they need their own keyboard-only ring.
 */
export const textButtonClassName =
  'rounded-md font-medium text-brand-600 hover:underline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-600';
