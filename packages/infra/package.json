{
  "name": "@hyperspace/infra",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "cdk synth --profile=hyperspace",
    "deploy": "cdk deploy --all --profile=hyperspace",
    "cdk": "cdk"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.130.0",
    "constructs": "^10.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "aws-cdk": "^2.130.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.4.0"
  }
}
