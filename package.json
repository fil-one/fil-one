{
  "name": "hyperspace",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "sst dev",
    "build": "npm run build -w packages/shared && npm run build -w packages/backend && npm run build -w packages/website",
    "deploy": "npm run build && sst deploy",
    "deploy:staging": "npm run build && sst deploy --stage staging",
    "deploy:production": "npm run build && sst deploy --stage production",
    "remove": "sst remove",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "sst": "^3",
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.3.0"
  },
  "engines": {
    "node": ">=20"
  }
}
